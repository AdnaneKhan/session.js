# P7-T2 — Tier-1 E2E suite

| Field | Value |
|---|---|
| Task | P7-T2 (plan §5 Phase 7) |
| Date | 2026-07-20 |
| Agent | agent:wave-final-e2e (worktree feat/voice-calls-e2e) |
| Status | **PASS** (offline; one networked attempt recorded) |

## Commands

```
cd calls && bun e2e/run-tier1.ts                    # offline suite, exit 0
cd calls && bun e2e/run-tier1.ts --real-timeouts    # nightly 60 s variant
SESSION_CALLS_NETWORK_TESTS=1 cd calls && bun e2e/run-tier1.ts --only lifecycle-networked --runs 3
```

## Offline suite — results (representative run, 2026-07-20)

```
scenario           mode     result  checks  ring  connect  audio1st  total  dur
lifecycle-full     offline  PASS    17      766   1566     1720      6507   6.5s
decline            offline  PASS    10      1     —        —         264    0.3s
ignore             offline  PASS    8       1     —        —         3010   3s
busy               offline  PASS    9       2     591      —         645    0.6s
timeout            offline  PASS    4       2     3007     —         3008   3s
reconnect-recover  offline  PASS    14      —     —        —         11     0s
reconnect-exhaust  offline  PASS    12      —     —        —         16     0s
multi-device       offline  PASS    9       2     532      —         2681   2.7s
wire-golden        offline  PASS    2       —     —        —         52     0.1s
redaction          offline  PASS    14      —     —        —         2684   2.7s

totals: 10 pass / 0 fail / 0 skip — 99 checks — 18.2s total
```

**Runtime budget: 18–20 s ≤ 5 min — met with enormous margin.** Three
offline runs performed during this wave (18.9 s, 18.2 s sample, 18.2 s);
all 10/10 PASS. Sample report:
`e2e/reports-sample/2026-07-20T18-19-41-818Z-tier1/report.json`,
sha256 `e45d60bdf90df60a3ac8fea811f799c2bd95536c357cccb663b0ffec9f9b3f27`.

Scenario semantics mirror the unit suite exactly (call-paths / call-manager
/ reconnect / multi-device tests): decline → END_CALL ×2 + `remote-declined`
both sides; ignore → **zero wire messages from the callee** (bus spy) +
caller `timeout` + missed `ignored`, no `ended` event; busy → missed
`busy` + END_CALL for the intruder's uuid, active call untouched;
timeout → END_CALL ×2, callee `remote-hangup` from the received END_CALL;
reconnect → initiator 5 s ×5 via fake-media injection through
`CallManagerDeps` (recovery mid-retry cancels the chain; exhaustion →
`ice-failed` + `IceFailureError` + END_CALL ×2); multi-device →
`answered-elsewhere` on device 2 with zero sends, audio flows on device 1;
redaction → no TURN creds / fingerprints / SDP bodies in any logger output
or event payload across a full REAL-media run.

The `--real-timeouts` flag switches the timeout scenario to the true
60 000 ms ring timeout (scenario guard 100 s) — wired for nightly.yml;
CI default is the fast 3 s variant.

## Networked lifecycle — ATTEMPTED (outcome recorded)

Two attempts, `SESSION_CALLS_NETWORK_TESTS=1`, `--runs 3`, fresh accounts:

**Attempt 1** (report `2026-07-20T18-14-39-419Z-tier1`):
- run 1: signaling+media connected, 0 audio frames, teardown stalled at
  END_CALL propagation — environmental (matches the P5-T2 relay-media
  degradation signature).
- run 2: same signature.
- run 3: **connected in 11,608 ms (≤ 15 000 S1 budget), 150/150 audio
  frames both directions.**
- Aggregate initially FAILed on an endReason DIRECTION bug in the
  harness's aggregate check (scenario hangs up from B; check asserted A
  hangs up) — fixed in `scenarios-tier1.ts`.

**Attempt 2** (report `2026-07-20T18-17-47-680Z-tier1`, post-fix):
- run 1, 2: same environmental signature (connected at signaling/media
  level, audio did not flow, END_CALL propagation stalled).
- run 3: **connected in 12,279 ms, 150/150 frames both directions,
  EndReasons local-hangup/remote-hangup.**
- **Scenario PASS (exit 0):** ≥1 run connected ≤ 15 s with audio verified
  both directions.

Assessment: the S1 connect budget is met on the working runs (11.6 s,
12.3 s — both ≤ 15 s) with the prior wave's 150/150 audio result
reproduced. The 2/3 per-attempt flake is the documented P5-T2 relay-media
degradation surfacing when ICE nominates a relay pair (the media binding
does not expose the selected pair type — `icePairType: "unknown"`, P5-T3
delta — so this attribution is by signature, not by candidate dump).
Networked reliability tracking continues in nightly.yml
(continue-on-error lane; plan: 3× consecutive greens before release).
