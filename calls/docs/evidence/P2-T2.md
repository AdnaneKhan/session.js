# P2-T2 — Timers & policies module (evidence)

| Field | Value |
|---|---|
| Task | P2-T2: Timers & policies module |
| File | `calls/src/policy.ts` |
| Branch | `feat/voice-calls-pkg` |
| Agent | swarm implementer (worktree agent-a057ee72dc35a677d) |
| Date (UTC) | 2026-07-20T14:52:05Z |

## Provenance (plan §10.4)

Written fresh from the published protocol facts (§3.1 freshness windows,
§3.2 self-send/batching/restart constants, §3.4 + Appendix B TURN config).
No lines copied from GPL/AGPL sources. SPDX: AGPL-3.0-or-later.

The TURN hosts/credentials are PUBLIC constants shipped in every official
Session client (session-android `PeerConnectionWrapper.kt`, session-desktop
`ts/session/constants.ts`); they are reproduced as data, not code.

## Verification

```
$ cd calls && bun test test/policy.test.ts
```

Tests (all deterministic; injected fake timers — no Bun.setSystemTime):

- Constants pinned exactly: TTL 300000, VERY_EXPIRED 900000, PRE_OFFER
  60000, call timeout 60000, ICE batch 200, boosted poll 500, restart
  interval 5000, restart max 5; Appendix B hosts (exact order) + credentials.
- `isFresh` boundaries: exactly-at is fresh (`≤`) for both windows;
  +1 ms over is stale; 15-min absolute drop for every kind; future
  timestamps (negative age) count as fresh with age clamped to 0
  (documented clock-skew tolerance); age-0 fresh.
- Self-send matrix for all 6 wire types (ANSWER/END_CALL allowed; others
  dropped).
- `IceCandidateBatcher` with a FakeClock: debounce (199 ms no flush, re-arm
  on enqueue, fires at exactly quiet+window), coalescing (3 candidates →
  ONE flush, order + parallel arrays preserved), flushNow (immediate +
  timer cancelled, no double flush; empty no-op), pending getter, dispose
  (drops pending, ignores post-dispose enqueues), re-usability, default
  200 ms window.
- `pickTurnServers`: 2 distinct servers from the official 5 with
  credentials (default rng); seeded rng `() => 0` → deterministic
  `[angus, hereford]`; seeded `() => 0.9999999` → identity shuffle
  `[freyr, angus]`; `defaultIceServers()` returns 2.
- `redactSensitive` (P5-T3 preview): masks `session202111` /
  `053c268164bc7bd7` wherever they appear; rewrites every
  `a=fingerprint:` SDP line to `a=fingerprint:[REDACTED]`; leaves clean
  text untouched.

Coverage: src/policy.ts — 100.00 % lines, 88.89 % funcs (the uncovered
functions are the default `setTimeout`/`clearTimeout` adapters, exercised
only when timers are NOT injected; policy tests deliberately use fake
timers per the task spec).

Suite sha256 / counts: see P2-T3.md (103 pass / 0 fail).
