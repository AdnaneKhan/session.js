# P7-T3 — Tier-2 fault & stress suite (nightly)

| Field | Value |
|---|---|
| Task | P7-T3 (plan §5 Phase 7) |
| Date | 2026-07-20 |
| Agent | agent:wave-final-e2e (worktree feat/voice-calls-e2e) |
| Status | **PASS** (offline suite; networked poll-latency gated to nightly) |

## Command & results (real run, 2026-07-20)

```
cd calls && bun e2e/run-tier2.ts          # exit 0

scenario               mode     result  checks  ring  connect  total  dur
signaling-loss         offline  PASS    11      —     —        2807   2.8s
poll-latency           offline  PASS    4       3002  6520     9188   9.2s
rapid-cycling          offline  PASS    49      —     —        347    0.3s
race-inbound-outbound  offline  PASS    9       —     —        8      0s

totals: 4 pass / 0 fail / 0 skip — 73 checks — 12.4s total
```

Report: `e2e/reports/2026-07-20T18-12-28-762Z-tier2/report.json`
(gitignored; numbers below are from its detail section).

## 1. signaling-loss (drop ~30% of swarm stores, seeded LCG)

5 lifecycle iterations, `callTimeoutMs: 8 000`, per-iteration guard —
assert: **connects or times out, never hangs; zero error events.**

```
iter  outcome     elapsed  stored  dropped  droppedByType
1     connected   590 ms   7       4        PRE_OFFER×1, ANSWER×1, END×2
2     connected   583 ms   7       1        END×1
3     connected   532 ms   7       0        —
4     connected   520 ms   7       2        ICE×1, ANSWER×1
5     connected   580 ms   7       1        PRE_OFFER×1
```

All 5 connected despite 30% loss — emergent protocol redundancy observed:
ANSWER is stored twice (peer + self-sync), so one drop survives; a
dropped PRE_OFFER is absorbed by the Desktop-tolerant OFFER path (OFFER
alone creates the inbound context); a dropped END_CALL only delays the
peer's view. Zero unhandled rejections. (Nightly expectation: the
distribution may include `ended:timeout` outcomes at this drop rate —
the assertion is termination, not success.)

## 2. poll-latency (3000 ms poll-cadence simulation)

Offline proxy: every swarm leg delayed by the poll interval (worst case
per leg). Baseline (no delay) vs 3000 ms:

```
baselineConnectMs:  521
degradedConnectMs:  6520
degradationMs:      5999
```

Lifecycle still connects (assert: < 45 s), clean hangup
(`local-hangup`). The ≈ +6 s = ~2 signaling legs × 3 s matches the
PRE_OFFER+OFFER → ANSWER round trip at 3 s cadence. The gated
`poll-latency-networked` scenario runs the same measurement over the
live swarm with real `Poller({ interval: 3000 })` instances
(nightly.yml, continue-on-error lane).

## 3. rapid-cycling (20× sequential place→hangup + extras)

- 20× fake-media cycles: every cycle ends `local-hangup`, `activeCall`
  undefined after each, exactly one end per cycle — 49 checks.
- **No leaked contexts/handles:** all 20 `FakeMediaSession`s closed
  (`media.sessions.every(s => s.closed)`), zero error events, and a 21st
  call succeeds (no `CallInProgressError` — the supervisor context map
  drains on teardown).
- 3× real werift media cycles (place → 100 ms → hangup before connect):
  clean ends, zero errors — PC create/teardown under rapid churn is
  leak-free.

## 4. race-inbound-outbound (A calls B while B calls A)

```
aEndReason: remote-declined     bEndReason: remote-declined
missedA: [busy]                 missedB: [busy]
ghostInboundContexts: 0
```

Both outbound calls resolve via the busy path: each side records
missed(busy) for the inbound and sends END_CALL for the OTHER's uuid
(both verified on the bus spy with exact uuid match); each outbound ends
`remote-declined` on the received END_CALL. No crash, zero error events,
both managers drained after dispose; the Desktop-tolerant
OFFER-after-busy ghost did not materialize in this run (race-dependent;
the scenario records the count and dispose cleans either outcome).

## Nightly wiring

`.github/workflows/nightly.yml` runs `run-tier2.ts` (offline) plus the
gated networked poll-latency lane, after `run-tier1.ts --real-timeouts`.
Flake budget per plan §6: 1 retry, tracked; 3× consecutive greens before
release.
