# P2-T1 — StateMachine module (evidence)

| Field | Value |
|---|---|
| Task | P2-T1: StateMachine module (FSM) |
| File | `calls/src/fsm/state-machine.ts` |
| Branch | `feat/voice-calls-pkg` |
| Agent | swarm implementer (worktree agent-a057ee72dc35a677d) |
| Date (UTC) | 2026-07-20T14:52:05Z |

## Provenance (plan §10.4)

Ported from session-android
`app/src/main/java/org/thoughtcrime/securesms/webrtc/data/StateMachine.kt`
(GPLv3), © Session Foundation, modified (Kotlin → TypeScript table-driven
port). Shipped under AGPL-3.0-or-later per D10. Header preserved in-file.

## Decisions / deviations (documented per task spec)

1. **Supplementary rows (2)** added to the Appendix C table, flagged
   `supplementary: true` in `CALL_FSM_TRANSITIONS`:
   - `{event: receive-answer, from: [local-pre-offer], to: connecting,
     effects: [set-remote-answer, drain-ice]}`
   - `{event: timeout, from: [local-pre-offer], to: disconnected,
     effects: [send-end-call-peer-if-local, send-end-call-self, reason-timeout]}`
   Rationale (in-file comment): the caller sends PRE_OFFER and OFFER
   back-to-back (Android `onOutgoingCall`) and may receive ANSWER — or hit
   the 60 s timeout — while the FSM is still in `local-pre-offer`; the
   `local-pre-offer` / `local-ring` distinction is not observable on the
   wire. The table therefore stays authoritative for the supervisor without
   modeling an internal promotion event. Table shape: 19 events / 21 rows.
2. **`inbound-while-busy` widened** to every state except `idle` and
   `disconnected` (Appendix C listed `[local-*, remote-*, connecting,
   connected]`). Per task guard spec; effect identical (`unchanged` +
   `record-missed` + `send-end-call-peer`); keeps the busy arbiter total
   during reconnect states. Documented in-file.
3. **Guards**: `restart-attempt` requires `ctx.isInitiator === true`,
   `receive-offer-restart` requires `ctx.isInitiator === false`; unsatisfied
   guard → `InvalidCallTransitionError` (same as an invalid pair).
4. `user-hangup` valid from `[local-pre-offer, local-ring, connecting,
   connected, pending-reconnect, reconnecting]` per task spec (callee
   hanging up while ringing is `user-decline` in the table — the supervisor
   maps `hangup()` in `remote-*` states to decline).

## Verification

```
$ cd calls && bun test test/fsm/matrix.test.ts --coverage
```

Exhaustive matrix: 10 states × 19 events = 190 pairs, each asserted against
the table (next-state + exact effect list) or `InvalidCallTransitionError`
where the table has no row. Plus: table-integrity tests (row/event/state/
effect membership, no ambiguous pairs, supplementary-row signature check),
hand-written spot-checks for every one of the 19 Appendix C events, guard
failure branches, unknown-event branch, and 12 scenario walks (full caller
flow incl. supplementary receive-answer, full callee flow, decline ×2,
ignore ×2, initiator reconnect, non-initiator reconnect, self-answer,
self-end-call, supplementary timeout, inbound-while-busy, invalid-send
state preservation, logger).

Coverage (bun --coverage; bun reports %Funcs/%Lines):

| File | % Funcs | % Lines |
|---|---|---|
| src/fsm/state-machine.ts | **100.00** | **100.00** |

All `transition()` branches exercised: row-found/not-found, initiator
guard pass/fail (incl. missing ctx), non-initiator guard pass/fail,
`unchanged` vs concrete target. Target met.

Combined suite sha256 (see P2-T3.md for full-suite numbers): test output
hash `f3bdcb94…7955` at 2026-07-20T14:52:05Z — 103 pass / 0 fail.
