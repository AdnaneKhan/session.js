# P4-T4 — Non-happy paths at the public CallManager level

| Field | Value |
|---|---|
| Task | P4-T4 (plan §5 Phase 4) |
| Date | 2026-07-20 |
| Agent | agent:wave3-api (worktree feat/voice-calls-api) |
| Status | **PASS — offline public-surface tests + networked decline/timeout** |

## Commands & exit codes

```
cd calls && bun test test/call-paths.test.ts                        # exit 0 — 9 pass
cd calls && SESSION_CALLS_NETWORK_TESTS=1 bun test test/integration/call-paths.test.ts
# exit 0 — 2 pass (real swarm)
```

## Offline (asserted through CallManager/Call, fake session)

- `reject()` → END_CALL peer AND self; ended `remote-declined`; not a missed call.
- `ignore()` → **zero wire messages**; missed recorded locally (`ignored`); no "ended".
- Inbound while busy → missed `busy` + END_CALL addressed to the intruder with
  the intruder's uuid; active call untouched; no incoming emitted.
- `callTimeoutMs: 30` → ended `timeout` + END_CALL peer+self after the injected
  30 ms (FakeTimers).
- 61 s-stale PRE_OFFER (injected clock) → missed `stale`, no incoming.
- Unapproved inbound → dropped silently, NO missed record; `requireApprovedContact: false` admits.
- `autoApproveOnCall: true` (default) → `acceptConversationRequest` strictly
  before the first call signaling (ordered action log); `false` → never called.
- CallImpl misuse: accept()/reject() on outbound reject with
  InvalidCallMessageError; ignore() throws — call untouched.

## Networked (SESSION_CALLS_NETWORK_TESTS=1, real swarm)

```
[paths] B declining
[paths] A ended: remote-declined
[paths] B ignoring (no response)
[paths] A ended after 5758 ms: timeout        (callTimeoutMs: 8000 on A)
 2 pass, 0 fail — 25.98 s
```

First run of the timeout test failed — the short `callTimeoutMs` had been
applied to B (callee) instead of A (caller). Test fixed (`makePair(optionsA,
optionsB)`); the package behavior was correct.
