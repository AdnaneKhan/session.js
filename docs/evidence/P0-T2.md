# P0-T2 — Test accounts & fixtures kit

- status: deferred-network
- agent: agent-ae9651a3c1a5c5c90
- timestamp: 2026-07-20T14:36:00Z

## Verification (scripts written; execution requires network + CI secrets)

- command (register): `bun scripts/register-account.ts [--mnemonic "..."] [--name "TestBot 1"]`
- command (smoke):
  `TEST_ACCOUNT_A="<mnemonic A>" TEST_ACCOUNT_B="<mnemonic B>" bun scripts/smoke-message.ts`
- exit code: not yet executed (deferred)
- output sha256: —

## Key excerpts

Scripts committed on `feat/voice-calls-docs`:

- `scripts/register-account.ts` — fresh mnemonic via `encode(generateSeedHex())`
  (`@session.js/mnemonic` + `@session.js/keypair`), Session boot (bun-network),
  `setMnemonic` → `setDisplayName` → self-message swarm registration, prints
  Session ID + mnemonic + message hashes; `--mnemonic` flag to reuse an
  existing identity. Header documents: network required, never commit
  mnemonics, CI stores them as `TEST_ACCOUNT_A/B` secrets.
- `scripts/smoke-message.ts` — reads `TEST_ACCOUNT_A`/`TEST_ACCOUNT_B` from
  env (exit 1 + usage if missing), boots two Session instances with separate
  `InMemoryStorage`, A sends random-nonce text to B, B polls at 500 ms until
  receipt (30 s timeout), asserts text match, prints message hash + latency,
  exits 0/1. Tagged `@network — skipped in offline CI lane`.

Static check in this lane: `bun scripts/smoke-message.ts` with env unset →
usage error, exit 1 (verified). `bunx tsc --noEmit` reports no errors for
`scripts/` (only the pre-existing `test/sending-and-polling.test.ts` Bun-API
type errors remain).

## Notes

- Execution path for the networked lane: run register-account.ts twice
  (accounts A and B), store the mnemonics as CI secrets `TEST_ACCOUNT_A/B`,
  then run smoke-message.ts; record message hash + latency here and flip
  status to `done`.
- Account lifecycle: swarm state persists; recreate accounts if polluted;
  rotate quarterly (plan R11).
