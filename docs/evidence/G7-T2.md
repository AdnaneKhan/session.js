# G7-T2 — Include groups in own config sends (+ multi-device round-trip)

- **Status:** done
- **Agent:** main (implementer)
- **Recorded:** 2026-07-21T14:24:25Z

## What

- **Core** `Session.sendConfigurationMessage({ activeClosedGroups })`
  (`src/instance/send-configuration-message.ts`): builds the legacy
  `ConfigurationMessage` (filling in our display name) with the given closed
  groups and stores it to **our own swarm** (namespace 0) with the **30-day
  config TTL** (spec §2.6 / GQ1 — `ConfigurationMessage.ttl()` now returns
  `TTL_DEFAULT.CONFIG_MESSAGE`).
- **Groups** `GroupManager.syncGroupsToLinkedDevices()`: collects active groups
  with their **latest** keypair and calls the session method (latest-only
  carry, spec §2.6 limitation).
- Round-trips through the bus to every linked device (endpoints sharing our id),
  driving the G7-T1 reconciliation.

## Verification

- command: `bun test test/config.test.ts` (groups)
- exit code: 0
- output sha256: `c29deb1d37d89ca9194e91433ae0cd97578244e324c8b5ecb4f0246b89c5adee`
- typecheck: groups + client publish both exit 0; full suite groups 57 / client 48 pass

## Key excerpts

```
 6 pass
 0 fail
```

G7-T2 cases: `syncGroupsToLinkedDevices` emits a config carrying the active
group with its latest keypair; **multi-device round-trip** — a second linked
device (attached after group creation, so it missed the NEW invite) joins the
group purely via the config sync and receives the same latest keypair.

## Notes

The modern libsession `UserGroups` wrapper (mechanism (b)) is **deferred to
v1.1** — decision gate `docs/escalations/G7-T3-usergroups-wrapper.md` (GQ4).
