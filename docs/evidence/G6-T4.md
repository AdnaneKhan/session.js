# G6-T4 — Fault tests (revocation/gate enforcement)

- **Status:** done
- **Agent:** main (implementer)
- **Recorded:** 2026-07-21T14:03:24Z

## What

Negative-path coverage for the legacy protocol's weak revocation and the
receive-side gates (forged mapped `groupUpdate` events fired at a manager):

- non-admin `MEMBERS_REMOVED` dropped on receive (and `sendRemoveMembers`
  throws `NotAnAdminError` on the send side);
- the first admin cannot be removed (send-side guard + receive-side drop);
- self-removal rejected (must leave);
- **stale update** (`timestamp <= lastJoinedTimestamp` watermark) dropped;
- update from a **non-member** dropped;
- **duplicate keypair** (same value) ignored by the registry;
- `ENCRYPTION_KEY_PAIR` from a **non-admin** dropped (no keypair appended).

## Verification

- command: `bun test test/members.test.ts` (groups)
- exit code: 0
- output sha256: `74c2eb987314f5ef843c695c912cc9c9e066073b269fae65f9bc8f392e25d35d`

## Key excerpts

```
 13 pass
 0 fail
```

All seven fault cases pass: members/name/keypair state is unchanged after each
forged invalid update, and the send-side guards throw the typed errors.

## Notes

Interop posture documented in user-facing docs (P9): removal is admin-only and
**revocation is weak by design** — a removed member keeps historical private
keys (can decrypt swarm content until 14-day expiry / pre-rotation), which is
why the removing admin rotates (§2.5). Official clients accept this.
