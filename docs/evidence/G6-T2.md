# G6-T2 — Remove (admin-only + rotation) and leave (zombies, admin-leave disband)

- **Status:** done
- **Agent:** main (implementer)
- **Recorded:** 2026-07-21T14:03:24Z

## What

- **`sendRemoveMembers`** (admin-only, §2.4): rejects non-admin
  (`NotAnAdminError`), self-removal, and removing the first admin; sends
  `MEMBERS_REMOVED` to the group swarm then **rotates** (§2.5): fresh x25519
  pair, a wrapper per *remaining* member sealed to their identity key,
  `ENCRYPTION_KEY_PAIR` to the group swarm sealed to the still-shared current
  key, new pair appended locally only after the send.
- **Inbound `#handleMembersRemoved`**: watermark + sender-∈-members gates;
  first-admin-removal dropped; **admin-only enforced on receive** (non-admin
  removals dropped); if *we* were removed → `#deleteClosedGroup` (stop polling,
  wipe keys/state); else update members + prune zombies.
- **`sendLeave`** (any member): `MEMBER_LEFT` to the group swarm, then delete
  locally. **Inbound `#handleMemberLeft`**: admin-left → disband for everyone
  (delete); self (other device) → delete; otherwise remove + add **zombie**.

## Verification

- command: `bun test test/members.test.ts` (groups)
- exit code: 0
- output sha256: `74c2eb987314f5ef843c695c912cc9c9e066073b269fae65f9bc8f392e25d35d`

## Key excerpts

```
 13 pass
 0 fail
```

G6-T2 cases: admin removes a member → admin holds 2 keypairs with a changed
latest, remaining member **unwraps its rotation wrapper** (2 keypairs, latest ==
admin's), removed member's group deleted (0 keypairs); member leave → leaver
deleted locally, others remove them + record a zombie; admin leave → group
disbanded for every manager.
