# G6-T3 — Rename + disappearing-message timer

- **Status:** done
- **Agent:** main (implementer)
- **Recorded:** 2026-07-21T14:03:24Z

## What

- **`sendRename(groupPubKey, newName)`** (any member, §2.4): validates a
  non-empty name, sends `NAME_CHANGE` to the group swarm (latest key), updates
  local state. Inbound `#handleNameChange` applies the same watermark +
  sender-∈-members gates and updates the name.
- **`expirationTimer`** (`deleteAfterSend` only; `deleteAfterRead` is rejected
  for groups upstream): carried in the NEW invite and re-applied on group chat
  sends (covered in G5-T1's deleteAfterSend test); `GroupState.expirationTimer`
  persists it.

## Verification

- command: `bun test test/members.test.ts` (groups)
- exit code: 0
- output sha256: `74c2eb987314f5ef843c695c912cc9c9e066073b269fae65f9bc8f392e25d35d`
- (deleteAfterSend send-path: G5-T1, `bun test test/chat.test.ts`)

## Key excerpts

```
 13 pass
 0 fail
```

G6-T3 case: any member renames the group → the other managers update the stored
name (watermark + member gates satisfied with the shared bus clock).
