# G5-T2 — Group receive: GroupPoller keypair provider + newest-first decrypt + senderIdentity

- **Status:** done
- **Agent:** main (implementer)
- **Recorded:** 2026-07-21T13:39:44Z

## What

Receive path via the core `GroupPoller` (built G2-T2, extended here):

- Decrypts each retrieved group-swarm message with the injected keypair
  registry, **newest-first** (the fixed `decryptForClosedGroup`), falling back
  through **historical (rotated-out) keypairs** so in-flight / pre-rotation
  messages still decrypt.
- Recovers the real author into `envelope.senderIdentity` and **drops our own
  messages**; delivers decoded `Content` to the consumer.
- Refactored into a `#tryDecrypt` result (`message | "own" | "skip" | "fail"`)
  shared by the fresh-message path and the retry path (G5-T3).

The groups `GroupManager` supplies the keypairs via
`getEncryptionKeyPairs`/`getLatestEncryptionKeyPair` and consumes the mapped
events (G5-T1).

## Verification

- command: `bun test test/group-poller.test.ts` (client)
- exit code: 0
- output sha256: `bd8ec8128f2bece9d28fd259ebfbbae105df2f6b3323e5c14a4f9a4044e57e0a`

## Key excerpts

```
 8 pass
 0 fail
```

G5-T2 cases: polls ns −10 and decrypts a group message, stamping
senderIdentity from the sealed box (source stays the group address);
**decrypts an in-flight message with a historical (rotated-out) key** when the
registry holds `[old, new]` and the message was sealed to `old` (newest-first
with fallback). Plus the underlying newest-first / fallback / no-mutation
behaviour proven at the crypto layer in G2-T1.
