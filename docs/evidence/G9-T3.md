# G9-T3 — NOTICE + root README roadmap + `@session.js/client@0.0.57-groups.1`

- **Status:** done
- **Agent:** main (implementer)
- **Recorded:** 2026-07-21T15:15:51Z

## What

- **Root `README.md`**: new "Closed-group additions (this fork)" section
  (lifecycle, `GroupManager`, MIT core patches, test coverage, v2/v3 +
  weak-revocation / no-onion note); credits + license table/intro now cover
  `groups/` alongside `calls/`.
- **`NOTICE`**: closed-groups section finalized — "contains code ported" (was
  "will contain"), provenance "filled in" (was "as porting lands"), combined
  work = client + `calls/` + `groups/`, and the fresh core-patch list extended
  with the groups MIT patches (`./crypto` export, `decryptForClosedGroup` fix,
  `GroupPoller` / `addGroupPoller`, group send/config/wrapper methods,
  `ClosedGroupControlMessage` schema + events, GroupContext, 30-day config TTL).
- **Version bump**: `@session.js/client` → `0.0.57-groups.1` (root
  `package.json`); `@session.js/groups` → `0.1.0` (G9-T2).
- **Roadmap checkbox**: `docs-site/docs/index.md` "Closed chats (legacy)" →
  done (group v2/v3 left as a future-work sub-item) — also recorded in G9-T1.

## Verification

- command: `grep '"version"' package.json groups/package.json`
- exit code: 0
- client version: `0.0.57-groups.1`; groups version: `0.1.0`

## Key excerpts

```
package.json:        "version": "0.0.57-groups.1",
groups/package.json: "version": "0.1.0",
```
