# G6-T1 — Add members (any member) + admin keypair-push

- **Status:** done
- **Agent:** main (implementer)
- **Recorded:** 2026-07-21T14:03:24Z

## What

`GroupManager.sendAddMembers(groupPubKey, membersToAdd[])` (spec §2.4):

- Guards: known / active / we-are-a-member (`NotAMemberError`); de-dupes
  already-members; enforces the 100-member limit.
- Sends `MEMBERS_ADDED` to the group swarm (latest key) + a `NEW` invite DM
  (latest keypair) to each newcomer.
- Inbound `#handleMembersAdded` (desktop `performIfValid` gates: known group,
  `timestamp > lastJoinedTimestamp` watermark, sender ∈ members): unions
  members, **prunes re-added members from zombies**, and — the §2.4 race fix —
  a **receiving admin pushes the latest keypair** to newcomers
  (`#pushLatestKeypair`: ENCRYPTION_KEY_PAIR reply DM with explicit publicKey +
  a wrapper sealed to the member's identity key).

Wrapper seal/open added to core Session (`sealKeypairWrapper` /
`openKeypairWrapper`, G2-T1 crypto) and to `GroupSessionLike`.

## Verification

- command: `bun test test/members.test.ts` (groups)
- exit code: 0
- output sha256: `74c2eb987314f5ef843c695c912cc9c9e066073b269fae65f9bc8f392e25d35d`

## Key excerpts

```
 13 pass  (full member-lifecycle suite; this file = G6-T1..T4)
 0 fail
```

G6-T1 cases: a non-admin member adds a newcomer → newcomer joins (NEW DM),
adder + receiving admin both update members, the receiving admin keypair-pushes
to the newcomer (reply DM with explicit publicKey), and the newcomer dedupes the
pushed keypair (exactly one); re-adding a former member (zombie) clears the
zombie flag across managers.
