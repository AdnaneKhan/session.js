# G8-T2 — Networked lifecycle (gated)

- **Status:** deferred-network (written; execution requires `SESSION_GROUPS_NETWORK_TESTS=1` + live swarm)
- **Agent:** main (implementer)
- **Recorded:** 2026-07-21T14:41:27Z

## What

- **Core** `Session.addGroupPoller({ groupPubKey, getEncryptionKeyPairs })` /
  `removeGroupPoller(handle)` (`src/instance/group-poller-manager.ts`): wires a
  core `GroupPoller` to the session's network (`Poll` request), `getSwarmsFor`,
  and storage; converts hex group keypairs → `SessionKeys`; routes decrypted
  group messages through the instance's mapped `groupUpdate` (control) /
  `message` (chat) events. This is the real-network receive path the
  `@session.js/groups` `GroupManager` drives through `GroupSessionLike`.
- **Networked lifecycle test** `groups/test/integration/lifecycle.test.ts`:
  fork↔fork over the live swarm with fresh throwaway accounts — create → join →
  group chat (exercises the real GroupPoller) → add member → remove+rotate.
  Gated behind `SESSION_GROUPS_NETWORK_TESTS=1` (skipped by default), 240 s
  timeout, intended for the nightly with continue-on-error.

## Verification

- command: `bun test test/integration/lifecycle.test.ts` (default env → skip)
- exit code: 0
- output sha256: `77fb09e526a22c2086f18393c87f8610df35f34842b02f4e722c629efea13f6d`
- client typecheck (`tsconfig.publish.json`): exit 0 (the addGroupPoller wiring
  compiles; the test's dynamic imports resolve lazily when the gate is on)

## Key excerpts

```
 0 pass
 1 skip
 0 fail
```

## Notes

- Execution deferred to a network-enabled lane (no live-swarm contact from
  this environment); the run command is recorded above. Official-client
  interop is the separate operator gate G8-T3.
