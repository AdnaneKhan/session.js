# G8-T1 — Offline E2E matrix (multi-manager bus)

- **Status:** done
- **Agent:** main (implementer)
- **Recorded:** 2026-07-21T14:41:27Z

## What

`groups/e2e/` — a 12-scenario offline E2E matrix over the multi-manager
`GroupBus` (run: `bun e2e/run-matrix.ts`, `--only <substr>` to filter; exits 0
iff all pass):

- S1 create+join (2 users); S2 group-chat round-trip; S3 bidirectional chat;
- S4 add member (3 users); S5 remove + **rotation** (removed member deleted,
  remaining member's latest key == remover's);
- S6 leave + zombie; S7 admin-leave disbands all; S8 rename propagates;
- S9 multi-device config sync (second linked device joins via config);
- S10 **concurrent add/remove rotation race** — admin removes C while a member
  adds D with no settle between; asserts membership converges on both managers
  (C out, D in) and chat still works after;
- S11 late joiner receives the **latest** (post-rotation) keypair via NEW;
- S12 removed member retains no keypairs (cannot read new chat).

## Verification

- command: `bun e2e/run-matrix.ts`
- exit code: 0
- output sha256: `1d9e508d27527587cfea11c5371fbf24c69a361e48fd15268123c6b4e5c905ec`

## Key excerpts

```
  [PASS] S1 … [PASS] S12
  12/12 scenarios passed
```

## Notes

The matrix exercises lifecycle/routing/gates/rotation deterministically at the
mapped-event level; the real seal/unseal path is covered at the core
(G2-T1/G5) and fork↔fork over the live swarm in G8-T2. Official-client interop
is the operator-run gate G8-T3.
