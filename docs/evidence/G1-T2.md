# G1-T2 — Closed-group golden wire fixtures

- **Status:** done
- **Agent:** main (implementer)
- **Recorded:** 2026-07-21T12:23:33Z

## What

Golden byte vectors for the six sendable legacy closed-group control types plus
the group chat (visible) message, generated by `scripts/generate-goldens.ts`
into `test/fixtures/wire/groups/` and verified by `scripts/verify-fixtures.ts`
(both scripts extended to cover the call + group fixtures). Provenance:
**computed** — derived from the published `SessionProtos.proto` field facts and
the pinned session-desktop (`master` @ `d86076b`) outgoing-message shapes;
protobuf encoding is deterministic, so any compliant encoder produces
byte-identical output. `ENCRYPTION_KEY_PAIR_REQUEST` (type 8) is unused by the
official clients and is not generated.

## Fixture sha256 (of `<hex>\n` file contents)

```
41c46361a5d8aa1a4208641013e3e5afe292f4be675b5312ce6b4c13b99c8279  groups/encryption-key-pair.hex
8fe8c8f06e41472d9d2abfc502b47d0a6e2169f5c3599d7b3942d9861529d9c0  groups/member-left.hex
dc40c65ee355a65d849e577cb8e501cd3d69c4806c69f1fd87ea5d9885ccc7c8  groups/members-added.hex
31073d95a2f375ec71500473618abe50616fcae9cd310d9892a19887b5a6d554  groups/members-removed.hex
745987f8ae428e071a1adfd079e5955b1dbee6450accecc935417c166575ff97  groups/name-change.hex
c48d99aceec99a241f6a7273cf0f96a65c3c8a9b850eff9190e5cd4be3395969  groups/new.hex
3f4bed9ed0046f16ff311f905eb407fcd9aac1b7fb04be9a005f33181c106541  groups/visible.hex
```

## Verification

- command: `bun scripts/generate-goldens.ts && bun scripts/verify-fixtures.ts`
- exit code: 0
- output sha256: `f9b36e8b1a6af4d57ae6e031d6bdca311673b83dc2bdfe1b91fe9f2f5bebc9c9`

## Key excerpts

```
OK  groups/new.hex  sha256=c48d99…
OK  groups/name-change.hex  sha256=745987…
OK  groups/members-added.hex  sha256=dc40c6…
OK  groups/members-removed.hex  sha256=31073d…
OK  groups/member-left.hex  sha256=8fe8c8…
OK  groups/encryption-key-pair.hex  sha256=41c463…
OK  groups/visible.hex  sha256=3f4bed…
all 12 fixtures verified
```

Each fixture is decoded, field-asserted against the canonical inputs (see
`test/fixtures/wire/README.md` "Closed groups" section), and re-encoded
byte-identically.

## Notes

- Canonical inputs are exported from `scripts/generate-goldens.ts`
  (`GROUP_PUBKEY`, `GROUP_MEMBER_A/B/C`, `GROUP_NAME`, `GROUP_ENC_PUB/PRIV`,
  `GROUP_WRAPPER_CIPHERTEXT`, `GROUP_BODY`, `GROUP_TIMESTAMP`) and re-imported by
  the verifier and `test/group-mapper.test.ts`, so a schema-class `contentProto()`
  that matches the golden is asserted byte-for-byte in G1-T1.
- `groups/visible.hex` confirms the `GroupContext.id = utf8("05…hex")` (66-byte)
  / `type = DELIVER` encoding for group chat messages.
