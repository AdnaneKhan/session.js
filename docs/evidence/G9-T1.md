# G9-T1 — docs-site "Closed groups" section

- **Status:** done
- **Agent:** main (implementer)
- **Recorded:** 2026-07-21T15:15:51Z

## What

New "Closed groups" tab in the docs-site (`docs-site/docs/closed-groups/`,
8 pages, wired into `mkdocs.yml` nav alongside "Voice calls"):

- `index.md` — overview + scope table + weak-revocation / no-onion warnings
- `setup.md` — the two pieces (`@session.js/client@0.0.57-groups.1` +
  `@session.js/groups@0.1.0`), runtimes, install, minimal wiring
- `creating.md` — `createGroup`, wire flow, rules
- `joining.md` — inbound `NEW` gates, update events, undecryptable retry
- `messaging.md` — `sendMessage` / `groupMessage`
- `members.md` — add / remove (admin-only + rotation) / leave / rename, state
- `api-reference.md` — full `GroupManager` API + `GroupState` + errors
- `protocol.md` — wire model, rotation, limits, and the **weak-revocation** and
  **no-onion** privacy disclosures (plan §2.4/§2.7; non-goals incl. the v1.1
  UserGroups deferral)

Also: roadmap checkbox updated in `docs-site/docs/index.md` ("Closed chats
(legacy)" → done, with a group-v2/v3 future-work sub-item) and the docs license
note now covers `groups/`.

## Verification

- command: `for f in index setup creating joining messaging members api-reference protocol; do test -f docs-site/docs/closed-groups/$f.md; done`
- exit code: 0 (all 8 pages present; mkdocs nav references all of them)

## Key excerpts

```
OK index.md … OK protocol.md   (8/8 present)
```
