# G4-T1 — Group key generation

- **Status:** done
- **Agent:** main (implementer)
- **Recorded:** 2026-07-21T13:26:07Z

## What

`src/keygen.ts` (AGPL, fresh, noble):

- `generateGroupAddress()` — random ed25519 keypair → public key converted
  ed→x25519 (`ed25519.utils.toMontgomery`) → 05-prefixed → 66-char hex group
  address. The ed25519 secret is **discarded** (spec §2.1: a legacy group
  cannot sign; no cryptographic membership proof).
- `generateEncryptionKeypair()` — fresh x25519 pair, stored **unprefixed**
  (32-byte hex), matching what group messages seal to and what rotates.

## Verification

- command: `bun test test/keygen.test.ts`
- exit code: 0
- output sha256: `3e52eecbba7dc250751f975c44bc81b766a71edc3c09bd6d0aa5a32595c80651`
- typecheck: `bunx tsc --project groups/tsconfig.json --noEmit` → exit 0

## Key excerpts

```
 4 pass
 0 fail
```

Tests: group address is 05-prefixed 66-char hex and random per call; encryption
keypair is unprefixed 64-char hex pub/priv and random per call.
