# P1-T2 — CallMessage schema class + wire goldens

- Task id: P1-T2
- Agent id: agent:wave1-core
- Date: 2026-07-20
- Branch: feat/voice-calls-core

## Summary

New `CallMessage` schema class (`src/messages/schema/call-message.ts`) extending
`ContentMessage`, mirroring `typing-indicator-message.ts` structure:

- `ttl()` → `Constants.TTL_DEFAULT.CALL_MESSAGE` (verified present:
  `grep TTL_DEFAULT node_modules/@session.js/consts/dist/index.d.ts` → `CALL_MESSAGE: number` = 300000 ms)
- `contentProto()` → `new SignalService.Content({ callMessage: { type, uuid, ...(sdps?.length && {sdps}), ...(sdpMLineIndexes?.length && {sdpMLineIndexes}), ...(sdpMids?.length && {sdpMids}) } })`
- Constructor validation (SessionValidationError / InvalidOptions): UUIDv4 regex
  `/^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i`; ICE_CANDIDATES
  requires non-empty sdps/sdpMLineIndexes/sdpMids of equal length; OFFER/ANSWER require non-empty sdps.
- Exported from `src/messages/schema/index.ts`.

Naming: the event payload type `CallMessage` in `src/messages/index.ts` keeps its name
(plan §4.4.2 — the schema class intentionally shares the name in its own module).

Golden fixtures: `scripts/generate-goldens.ts` (committed) generates the 5 canonical
Content encodings and writes `test/fixtures/wire/{pre-offer,offer,answer,ice-candidates,end-call}.hex`
plus a provenance `README.md`. Generated hex matched the pre-verified expected values EXACTLY.

Provenance: written fresh from the published SessionProtos.proto field facts. No lines
copied from GPL/AGPL sources. Licensable under MIT for upstream contribution.

## Files

- New: `src/messages/schema/call-message.ts`
- Modified: `src/messages/schema/index.ts`
- New: `scripts/generate-goldens.ts`
- New: `test/fixtures/wire/{pre-offer,offer,answer,ice-candidates,end-call}.hex`, `test/fixtures/wire/README.md`
- Test: `test/wire/call-message-encode.test.ts`

## Commands & results

```
$ grep TTL_DEFAULT node_modules/@session.js/consts/dist/index.d.ts
export declare const TTL_DEFAULT: {
    CALL_MESSAGE: number;   # 5 minutes
...
```

```
$ bun run scripts/generate-goldens.ts
pre-offer.hex  sha256=dc20a53e192b102e621700346b82db15356e7e0b7da7765e7112d031cc3cc75f
  1a2808062a2431313131313131312d313131312d343131312d383131312d313131313131313131313131
offer.hex  sha256=024f6d462efc5fb53ad4ccf2b6cca45bc4df6f4618f2bb721d66ce7e2809a1d6
  1a550801122b763d300d0a...0d0a2a2431313131313131312d313131312d343131312d383131312d313131313131313131313131
answer.hex  sha256=6329bad3729c28f4a1e2fefe2c50db27eeb88ce898690fdadd4d14e4b93e9b6b
ice-candidates.hex  sha256=5c6b7c5261026fb2624f5626fe408300eabf2e25db495d9cd847c6bb9ada0beb
end-call.hex  sha256=b5c1727957103d00f67772bfbb4ef55226967dffe4abc434c13723b9e8eaac97
exit code: 0
```

All 5 hex values byte-identical to the expected values in the task brief.

```
$ bun test test/wire/call-message-encode.test.ts
 9 pass
 0 fail
Ran 9 tests across 1 files.
exit code: 0
```

Tests construct the schema CallMessage for each of the 5 shapes (CallMessage proto has NO
timestamp field → encoding deterministic), compare
`Buffer.from(msg.plainTextBuffer()).toString("hex")` to committed fixture hex, assert
ttl() = 300000, and assert validation throws (bad uuid, non-v4 uuid, mismatched/empty ICE
parallel arrays, OFFER/ANSWER without sdps).

```
$ bun run build
exit code: 0
```

Full suite: `bun test` → 38 pass / 1 fail (pre-existing live-network test only).
