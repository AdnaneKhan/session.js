# P0-T4 — Protocol-notes doc

- status: done
- agent: agent-ae9651a3c1a5c5c90
- timestamp: 2026-07-20T14:36:00Z

## Verification

- command: `wc -l docs/protocol-notes.md`
- exit code: 0
- output sha256: — (document artifact; see commit on `feat/voice-calls-docs`)

## Key excerpts

```
379 docs/protocol-notes.md
```

`docs/protocol-notes.md` written as the living specification consolidating
plan §3.1–3.6: wire schema (CallMessage proto + Content/Envelope field
numbers), TTL=300000 rule, self-send rules (only ANSWER/END_CALL), freshness
windows (TTL / 15-min absolute / 60-s PRE_OFFER), multi-device semantics
(swarm fan-out, answer races via self-ANSWER), data channel ("signaling",
negotiated id 548, `{"hangup":true}` sent, both keys accepted), normative
call flow (caller/callee/both), access gates (contacts-only, opt-in beta,
1:1), media (stock DTLS-SRTP, 5 TURN hosts + static public creds,
P2P-first/relay fallback, Plan-B↔Unified interop risk + F1/F2 fallbacks),
session.js current-state summary, reference-implementation file map
(plan Appendix E), glossary (Appendix F), and open questions (plan §9 Q1–Q7,
all status "open" with owners). Header carries the living-document notice;
primary sources cited as in plan §3.

## Notes

- DoD: doc exists with §3 content + open-questions section — satisfied.
- Living document: discrepancies discovered during implementation must be
  corrected here and recorded in this directory.
